import 'package:flutter/material.dart';

class UserTeamsScreen extends StatelessWidget {
  const UserTeamsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column(children: [Text('3')])),
    );
  }
}
